<div class="container">
  <h1 class="my-2">Genres</h1><h2>list& new registrations</h2>
  <div class="row">
    <div class="col-6 my-5">
      <%= form_with model: @genre, url: admin_genres_path, method: :post do |f| %>
        <%= f.label :name %>
        <%= f.text_field :name %>
        <%= f.submit 'New Registration',class:"btn btn-success" %>
    　<% end %>
    </div>
    <div class="col-6">
      <table class="table">
        <thead>
          <tr>
            <th>List</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% @genres.each do |genre| %>
          <tr>
            <td><%= genre.name %></td>
            <td><%= link_to "Edit",edit_admin_genre_path(genre.id), class:"btn btn-success" %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>