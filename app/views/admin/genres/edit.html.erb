<h1>Genres<br>Rdit</h1>

  <%= form_with model: @genre, url: admin_genre_path(@genre) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.submit 'Save',class:"btn btn-success" %>
  <% end %>
