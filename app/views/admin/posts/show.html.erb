<h1>Admin::Posts#show</h1>
<h1>Posts#show</h1>

  <%= image_tag @post.get_image ,size: "400x400" %>
  <table>
    <tbody>
      <tr>
        <th>Genre</th>
        <td><%= @post.genre.name %></td>
      </tr>
      <tr>
        <th>Title</th>
        <td><%= @post.title %></td>
      </tr>
      <tr>
        <th>Explain</th>
        <td><%= @post.body %></td>
      </tr>
      <tr>
        <th>Ingredient</th>
        <td><%= @post.ingredient %></td>
      </tr>
      <tr>
        <th>Method</th>
        <td><%= @post.method %></td>
      </tr>
      <tr>
        <th>Level</th>
        <td><%= @post.level %></td>
      </tr>
      <tr>
        <th>Originality</th>
        <td><%= @post.originality %></td>
      </tr>
    </tbody>
  </table>
  </br>
  　<%= link_to "Destroy",admin_post_path(@post), method: :delete , data: { confirm: 'Do you really want to delete it?' },class:"btn btn-danger" %></td>
  　
  　<p>Comments: <%= @post.comments.count %></p>
    <% @post.comments.each do |comment| %>
      <%= comment.user.nickname %></br>
      <%= comment.created_at.strftime('%Y/%m/%d') %></br>
      <%= comment.body %></br>
      <%= link_to "destroy", post_comment_path(comment.post,comment), method: :delete %></br>
    <% end %>